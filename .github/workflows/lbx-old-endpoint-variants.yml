name: LBX Old Endpoint Variants
on:
  push:
    branches: [tmp/lbx-poi-crawl-20260907]
    paths: [scripts/lbx_old_endpoint_variants.py, .github/workflows/lbx-old-endpoint-variants.yml]
permissions: {contents: write}
jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 4
    steps:
      - uses: actions/checkout@v4
        with: {ref: tmp/lbx-poi-crawl-20260907}
      - uses: actions/setup-python@v5
        with: {python-version: '3.12'}
      - run: python -m pip install --disable-pip-version-check requests
      - run: python scripts/lbx_old_endpoint_variants.py
      - run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add old_endpoint_variants_output
          git commit -m "Add LBX endpoint parameter results [skip ci]" || exit 0
          git pull --rebase origin tmp/lbx-poi-crawl-20260907
          git push origin HEAD:tmp/lbx-poi-crawl-20260907
